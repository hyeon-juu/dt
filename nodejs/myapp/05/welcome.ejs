<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8" />
    <title>EJS 예제</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <h1>환영합니다, <%= name %>님! (<%= user %>)</h1>
    <ul>
        <% for(let i of rows){ %>
            <li><%= i.hg_nm %>(<%= i.hj_nm %>) / <%= i.poly_nm %> / <%= i.orig_nm %></li>
        <% } %>
    </ul>

    <!-- for(let i=0;i<list.length;i++){} -->
<table border="1"  class="table table-bordered">
    <tr>
    <td>대수</td>
    <td>이름</td>
    <td>성별</td>
    <td>소속</td>
    <td>지역구</td>
  </tr>

    <% rows.forEach((v)=>{ %>
        <tr>
            <td><%= v.sch_unit_cd %></td>
            <td><%= v.hg_nm %></td>
            <td><%= v.sex_gbn_nm %></td>
            <td><%= v.poly_nm %></td>
            <td><%= v.orig_nm %></td>
        </tr>

    <% }) %>
    </table>
</body>

</html>